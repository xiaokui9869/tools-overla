# Android 16 悬浮窗应用开发指南

## 目录
1. [项目概述](#项目概述)
2. [开发环境准备](#开发环境准备)
3. [项目创建步骤](#项目创建步骤)
4. [代码结构说明](#代码结构说明)
5. [功能实现详解](#功能实现详解)
6. [运行与测试](#运行与测试)
7. [常见问题解答](#常见问题解答)

## 项目概述

本项目是一个 Android 16 (API 36) 应用程序，使用 Kotlin 语言开发，实现了悬浮窗功能，可以显示实时时间和电量信息。应用具有以下特点：

- 检测并请求悬浮窗权限
- 显示半透明黑色背景的悬浮窗
- 实时显示系统时间（HH:mm:ss 格式）
- 显示当前电量，并根据电量级别动态改变颜色
  - 电量 > 60%：绿色
  - 电量 20%-60%：黄色
  - 电量 < 20%：红色
- 长按悬浮窗 1.5 秒可关闭应用
- 支持点击切换横版/竖版显示

## 开发环境准备

### 必需软件
1. **Android Studio Panda (2024.3+)**：下载并安装最新版本的 Android Studio
2. **JDK 17**：Android Studio Panda 通常自带 JDK 17，如果没有，请单独安装
3. **Android SDK**：确保安装了 Android 16 (API 36) SDK

### SDK 安装步骤
1. 打开 Android Studio
2. 进入 Tools -> SDK Manager
3. 在 "SDK Platforms" 选项卡中，勾选 Android 16 (API 36)
4. 在 "SDK Tools" 选项卡中，确保安装了以下工具：
   - Android SDK Build-Tools
   - Android SDK Platform-Tools
   - Android Emulator
5. 点击 "Apply" 安装所需的 SDK 组件

## 项目创建步骤

### 步骤 1：创建新项目
1. 打开 Android Studio
2. 点击 "New Project"
3. 选择 "Empty Activity" 模板
4. 点击 "Next"

### 步骤 2：配置项目
1. **Name**：输入 "MyApplication"（或您喜欢的名称）
2. **Package name**：输入 "com.chronopower.myapplication"
3. **Save location**：选择项目保存位置
4. **Language**：选择 "Kotlin"
5. **Minimum SDK**：选择 "API 28: Android 9 (Pie)"
6. **Build configuration**：选择 "Kotlin DSL (build.gradle.kts)"
7. 点击 "Finish" 创建项目

### 步骤 3：等待项目构建完成
Android Studio 会自动下载依赖并构建项目，这可能需要几分钟时间。

## 代码结构说明

### 项目目录结构
```
app/
├── src/
│   └── main/
│       ├── AndroidManifest.xml          # 应用清单文件
│       ├── java/com/chronopower/myapplication/
│       │   ├── MainActivity.kt          # 主活动
│       │   └── FloatingWindowService.kt # 悬浮窗服务
│       └── res/
│           ├── drawable/                # 图片资源
│           ├── layout/
│           │   └── floating_window_layout.xml  # 悬浮窗布局
│           ├── mipmap-*/                # 应用图标
│           ├── values/
│           │   ├── colors.xml           # 颜色资源
│           │   ├── strings.xml          # 字符串资源
│           │   └── themes.xml           # 主题资源
│           └── xml/                     # XML配置文件
└── build.gradle.kts                     # 应用级构建文件
```

### 核心文件说明

#### 1. AndroidManifest.xml
应用清单文件，声明应用的组件、权限和配置。主要包含：
- 权限声明（悬浮窗权限、前台服务权限）
- 主活动声明
- 悬浮窗服务声明

#### 2. MainActivity.kt
主活动类，负责：
- 检测悬浮窗权限
- 显示权限请求对话框
- 启动悬浮窗服务

#### 3. FloatingWindowService.kt
悬浮窗服务类，负责：
- 创建和管理悬浮窗
- 显示实时时间和电量信息
- 处理长按事件关闭应用

#### 4. floating_window_layout.xml
悬浮窗布局文件，定义悬浮窗的UI外观。

## 功能实现详解

### 1. 权限请求实现

#### AndroidManifest.xml 中的权限声明
```xml
<!-- 悬浮窗权限：允许应用在其他应用之上显示窗口 -->
<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

<!-- 前台服务权限：Android 9.0+ 需要此权限来启动前台服务 -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

<!-- Android 14+ 需要声明前台服务类型 -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
```

#### MainActivity.kt 中的权限检查与请求
```kotlin
// 检查是否已获取悬浮窗权限
private fun checkOverlayPermission(): Boolean {
    return if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
        // Android 6.0及以上使用Settings.canDrawOverlays方法检查权限
        Settings.canDrawOverlays(this)
    } else {
        // Android 6.0以下默认有权限
        true
    }
}

// 请求悬浮窗权限
private fun requestOverlayPermission() {
    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
        // Android 6.0及以上需要请求悬浮窗权限
        val intent = Intent(
            Settings.ACTION_MANAGE_OVERLAY_PERMISSION,
            Uri.parse("package:$packageName")
        )
        // 使用ActivityResultLauncher启动权限请求
        overlayPermissionLauncher.launch(intent)
    } else {
        // Android 6.0以下默认有权限，直接启动悬浮窗服务
        startFloatingWindowService()
    }
}
```

#### 权限请求对话框
```kotlin
private fun showPermissionRequestDialog() {
    AlertDialog.Builder(this)
        .setTitle("权限请求")  // 对话框标题
        .setMessage("本应用需要申请悬浮窗权限以运行")  // 对话框内容
        .setPositiveButton("确认") { _, _ ->
            // 点击确认按钮，跳转到系统权限设置页面
            requestOverlayPermission()
        }
        .setNegativeButton("取消") { dialog, _ ->
            // 点击取消按钮，关闭对话框
            dialog.dismiss()
            // 退出应用
            finish()
        }
        .setCancelable(false)  // 不允许点击对话框外部关闭
        .show()  // 显示对话框
}
```

### 2. 悬浮窗实现

#### 悬浮窗布局 (floating_window_layout.xml)
```xml
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#CC000000"  <!-- 半透明黑色背景 -->
    android:padding="16dp"  <!-- 内边距 -->
    android:gravity="center">  <!-- 内容居中 -->

    <!-- 时间显示 -->
    <TextView
        android:id="@+id/tv_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🕛 00:00:00"
        android:textColor="#FFFFFF"  <!-- 白色文字 -->
        android:textSize="18sp"  <!-- 字体大小 -->
        android:textStyle="bold"  <!-- 粗体 -->
        android:layout_marginBottom="8dp"  <!-- 底部边距 -->
        android:gravity="center" />

    <!-- 电量显示 -->
    <TextView
        android:id="@+id/tv_battery"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔋 100%"
        android:textColor="#4CAF50"  <!-- 初始为绿色 -->
        android:textSize="16sp"  <!-- 字体大小 -->
        android:textStyle="bold"  <!-- 粗体 -->
        android:gravity="center" />
</LinearLayout>
```

#### 悬浮窗服务 (FloatingWindowService.kt)
```kotlin
// 添加悬浮窗到窗口
private fun addFloatingWindow() {
    // 设置悬浮窗的布局参数
    val params = WindowManager.LayoutParams(
        WindowManager.LayoutParams.WRAP_CONTENT,  // 宽度为内容自适应
        WindowManager.LayoutParams.WRAP_CONTENT,  // 高度为内容自适应
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY  // Android 8.0+使用此类型
        } else {
            @Suppress("DEPRECATION")
            WindowManager.LayoutParams.TYPE_PHONE  // Android 8.0以下使用此类型
        },
        WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE,  // 不获取焦点，不影响其他应用操作
        PixelFormat.TRANSLUCENT  // 支持透明背景
    )

    // 设置悬浮窗的位置（屏幕右上角）
    params.gravity = Gravity.TOP or Gravity.END
    params.x = 100  // 距离右边的距离
    params.y = 100  // 距离顶部的距离

    // 添加悬浮窗到窗口
    windowManager.addView(floatingView, params)
}
```

### 3. 实时数据显示

#### 时间更新
```kotlin
// 用于更新时间的Handler
private val timeHandler = Handler(Looper.getMainLooper())
// 用于更新时间的Runnable
private val timeRunnable = object : Runnable {
    override fun run() {
        updateTime()  // 更新时间显示
        timeHandler.postDelayed(this, 1000)  // 每秒更新一次
    }
}

// 更新时间显示
private fun updateTime() {
    // 获取当前时间
    val currentTime = Date()
    // 创建时间格式化器
    val dateFormat = SimpleDateFormat("HH:mm:ss", Locale.getDefault())
    // 格式化时间
    val timeString = dateFormat.format(currentTime)
    // 更新时间显示
    tvTime.text = "🕛 $timeString"
}
```

#### 电量更新
```kotlin
// 更新电量显示
private fun updateBattery() {
    // 获取电池状态
    val batteryStatus: Intent? = IntentFilter(Intent.ACTION_BATTERY_CHANGED).let { ifilter ->
        registerReceiver(null, ifilter)
    }

    // 获取电量级别（0-100）
    val batteryLevel: Int = batteryStatus?.getIntExtra(BatteryManager.EXTRA_LEVEL, -1) ?: -1
    // 获取电池电量比例（0-1）
    val batteryScale: Int = batteryStatus?.getIntExtra(BatteryManager.EXTRA_SCALE, -1) ?: -1

    // 计算电量百分比
    val batteryPercent: Float = if (batteryLevel >= 0 && batteryScale > 0) {
        batteryLevel * 100 / batteryScale.toFloat()
    } else {
        50f  // 默认50%
    }

    // 更新电量显示
    tvBattery.text = "🔋 ${batteryPercent.toInt()}%"

    // 根据电量设置颜色
    when {
        batteryPercent > 60 -> {
            // 电量大于60%，显示绿色
            tvBattery.setTextColor(0xFF4CAF50.toInt())
        }
        batteryPercent >= 20 -> {
            // 电量在20%-60%之间，显示黄色
            tvBattery.setTextColor(0xFFFFEB3B.toInt())
        }
        else -> {
            // 电量小于20%，显示红色
            tvBattery.setTextColor(0xFFF44336.toInt())
        }
    }
}
```

### 4. 长按关闭应用实现

```kotlin
// 长按检测相关变量
private var isLongPress = false  // 是否正在长按
private val longPressDuration = 1500  // 长按时间阈值（毫秒）
private val longPressHandler = Handler(Looper.getMainLooper())
// 长按检测的Runnable
private val longPressRunnable = Runnable {
    isLongPress = true
    // 长按1.5秒后，关闭悬浮窗并退出应用
    closeFloatingWindowAndExit()
}

// 处理悬浮窗的触摸事件
private fun handleTouchEvent(view: View, event: MotionEvent): Boolean {
    when (event.action) {
        MotionEvent.ACTION_DOWN -> {
            // 手指按下时，记录初始位置和时间
            initialX = event.rawX.toInt()
            initialY = event.rawY.toInt()
            initialTouchTime = System.currentTimeMillis()
            isLongPress = false

            // 启动长按检测
            longPressHandler.postDelayed(longPressRunnable, longPressDuration.toLong())
            return true
        }
        MotionEvent.ACTION_MOVE -> {
            // 手指移动时，如果移动距离过大，取消长按检测
            val currentX = event.rawX.toInt()
            val currentY = event.rawY.toInt()
            val dx = Math.abs(currentX - initialX)
            val dy = Math.abs(currentY - initialY)

            if (dx > 10 || dy > 10) {
                // 移动距离超过10像素，取消长按检测
                longPressHandler.removeCallbacks(longPressRunnable)
            }
            return true
        }
        MotionEvent.ACTION_UP -> {
            // 手指抬起时，取消长按检测
            longPressHandler.removeCallbacks(longPressRunnable)

            // 如果不是长按，可以在这里处理点击事件
            if (!isLongPress) {
                // 点击事件处理（如果需要）
            }
            return true
        }
    }
    return false
}

// 关闭悬浮窗并退出应用
private fun closeFloatingWindowAndExit() {
    try {
        // 移除悬浮窗
        windowManager.removeView(floatingView)
    } catch (e: Exception) {
        e.printStackTrace()
    }

    // 停止服务
    stopSelf()

    // 退出应用
    android.os.Process.killProcess(android.os.Process.myPid())
}
```

## 运行与测试

### 在模拟器上运行
1. 在 Android Studio 中，点击工具栏上的 "Device Manager"
2. 点击 "Create Device" 创建新的虚拟设备
3. 选择设备型号（如 Pixel 6）
4. 选择系统镜像（Android 16）
5. 完成设备创建
6. 点击工具栏上的 "Run" 按钮（绿色三角形）或按 Shift+F10

### 在真机上运行
1. 在手机上启用"开发者选项"和"USB调试"
2. 使用 USB 数据线连接手机和电脑
3. 在手机上授权电脑进行 USB 调试
4. 在 Android Studio 中，选择您的设备作为运行目标
5. 点击工具栏上的 "Run" 按钮（绿色三角形）或按 Shift+F10

### 测试步骤
1. 首次运行应用时，应该会看到权限请求对话框
2. 点击"确认"按钮，跳转到系统权限设置页面
3. 授予悬浮窗权限
4. 返回应用，应该会看到悬浮窗显示在屏幕上
5. 悬浮窗应该显示当前时间和电量信息
6. 长按悬浮窗 1.5 秒，应用应该会关闭
7. 点击悬浮窗可以切换横版/竖版显示

## 常见问题解答

### Q1: 为什么应用启动后没有显示悬浮窗？
A: 请确保已授予应用悬浮窗权限。如果没有，请按照以下步骤操作：
1. 打开手机的"设置"应用
2. 找到"应用管理"或"应用"选项
3. 找到并点击您的应用
4. 点击"权限"或"应用权限"
5. 找到"悬浮窗"或"在其他应用上层显示"权限
6. 将其设置为"允许"

### Q2: 为什么悬浮窗显示的位置不对？
A: 悬浮窗的初始位置是在代码中设置的，您可以在 FloatingWindowService.kt 文件中的 addFloatingWindow() 方法中修改 params.x 和 params.y 的值来调整悬浮窗的位置。

### Q3: 为什么长按悬浮窗 1.5 秒后应用没有关闭？
A: 请确保您在长按过程中没有移动手指。如果移动距离超过 10 像素，长按检测会被取消。您可以调整 FloatingWindowService.kt 文件中的 dx > 10 || dy > 10 这行代码中的阈值来改变移动距离的限制。

### Q4: 为什么电量显示不准确？
A: 电量信息是从系统获取的，如果显示不准确，可能是系统电量信息未及时更新。您可以尝试重启设备或检查设备电池状态。

### Q5: 如何修改悬浮窗的样式？
A: 您可以修改 floating_window_layout.xml 文件来改变悬浮窗的样式，例如背景颜色、字体大小、内边距等。

### Q6: 如何适配不同版本的 Android？
A: 代码中已经使用了 Build.VERSION.SDK_INT 来判断 Android 版本，并根据不同版本使用不同的 API。例如，悬浮窗类型在 Android 8.0 及以上使用 TYPE_APPLICATION_OVERLAY，而在 Android 8.0 以下使用 TYPE_PHONE。

### Q7: 为什么在 Android 16 上需要声明前台服务类型？
A: 从 Android 14 开始，系统要求所有前台服务必须声明服务类型。在 Android 16 中，这一要求更加严格。我们在 AndroidManifest.xml 中为 FloatingWindowService 声明了 foregroundServiceType="specialUse"，并在代码中创建了通知渠道和通知，以满足 Android 16 的要求。

### Q8: 如何切换悬浮窗的横版/竖版显示？
A: 点击悬浮窗即可在横版和竖版显示之间切换。此功能允许您根据屏幕空间和需求调整悬浮窗的布局方向。

## 总结

本指南详细介绍了如何开发一个 Android 16 悬浮窗应用，包括权限请求、悬浮窗实现、实时数据显示、长按关闭应用和横版/竖版切换等功能。通过学习本指南，您应该能够：
- 创建一个 Android 项目
- 请求和处理悬浮窗权限
- 创建和管理悬浮窗
- 显示实时时间和电量信息
- 实现长按关闭应用的功能
- 实现横版/竖版切换功能
- 适配不同版本的 Android，特别是 Android 16

希望这个指南对您有所帮助！如果您有任何问题或建议，欢迎随时提出。
